import {css} from '@emotion/react';

export default css`
  body {
    height: initial;
    min-height: 100%;
  }
  #root {
    display: flex;
    background: #f0f2f5;
    flex-direction: column;
    min-height: 100vh;
  }
`;
