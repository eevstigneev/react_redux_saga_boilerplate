import styled from '@emotion/styled';
import {Menu} from 'antd';

export const MenuStyled = styled(Menu)`
  height: inherit;
`;

export const MenuItemStyled = styled(Menu.Item)``;
export const ItemGroupStyled = styled(Menu.ItemGroup)``;
